// import { useEffect } from "react";
// import { useDispatch, useSelector } from "react-redux";
// import { getTracks } from "../../redux/trucks/operations.js";

const Items = () => {
  // const filters = useSelector((state) => state.trucks.filtres);
  // const dispatch = useDispatch();
  // const [page, setPage] = useState(1);
  // const [loadingMore, setLoadingMore] = useState(false); //loader from more btn

  // useEffect(() => {
  //   dispatch(getTracks({ page: 1, limit: 4, filters }));
  // }, [dispatch, filters]);

  // useEffect(() => {
  //   if (page > 1) {
  //     dispatch(getTrucksMore({ page, limit: 4, filters }));
  //   }
  // }, [dispatch, page, filters]);

  // const handleShowNextTruck = () => {
  //   setPage(page + 1);
  // };

  return <div>Items</div>;
};

export default Items;
